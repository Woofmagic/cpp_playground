// Instructions for the preprocessor:
#include <iostream>

// *Use* the *namespace* `std`:
// :: now, you don't have to "qualify" `std` elements:
using namespace std;

// (type) and (name) for functions:
// :: main() is how all C++ start
int main()
{
    cout << "Hello World!";
    cout << "Another sentence injected into character output device.";
}